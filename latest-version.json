{
    "version": "1.1.173",
    "downloadUrl": "https://github.com/novapanel-ai/novapanel-updates/releases/download/v1.1.173/NovaPanel-v1.1.173.zip",
    "notes": [
        "Fixed a real defect introduced with the new sensor service: telemetry could start twice. Making startMod3() async (to try the service pipe first) silently defeated its 'already running' guard, because the guard checks a value that is now…",
        "System Scan no longer tells you to report a bug when the sensor service simply reconnects. A dropped pipe left the recorded state as 'connected', so the scan took its service-is-running branch and said the missing reading 'shouldn't…",
        "Fixed corrupted characters throughout the app. Text that should read '—' was displaying as 'â€”' in about 600 places, including game titles used for session detection: '3DMark — Time Spy' and '3DMark — System Info' could never be matched…",
        "Sensor service hardening (Windows service): fixed three faults that only appear under real conditions. Broadcast writes from its three independent producer threads were not serialised, so two lines could interleave into one corrupt line on…",
        "Fixed sensor path resolution crashing outside the app runtime, which took out several test suites and would have affected any non-Electron tooling."
    ],
    "required": false
}
