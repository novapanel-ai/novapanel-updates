{
    "version": "1.1.536",
    "downloadUrl": "https://github.com/novapanel-ai/novapanel-updates/releases/download/v1.1.536/NovaPanel-v1.1.536.zip",
    "notes": [
        "⚠️ THE TOGGLE KNOB WAS NEVER OFF CENTER — IT WAS TOO BIG. The design system puts a 2.4px border on that ornament, and CSS `width` reports the CONTENT box, so every measurement I took read a perfectly centered 14px knob while what actually…",
        "The OLED shift toggle turns the same teal as its label, so the row reads as one statement instead of a teal label wearing the generic gold.",
        "⚠️ SHORTENING THE EDITOR NO LONGER SCROLLS THE WHOLE SCREEN. Each screen now fills the content area and each COLUMN scrolls inside itself, so a short window scrolls only the column that is genuinely too tall — the map stays fully visible…",
        "The Position footer gained the display's effective resolution beside the scaling — what a window actually gets to lay out in (2048×1280 on a 2560×1600 screen at 1.25×), read off the display's own bounds rather than divided, so it stays…",
        "The editor can no longer be sized into a scroll: its height floor was 400px, less than two thirds of what the shortest screen needs. Measured on the live window — Position needs 671px, Layers 673px, Configure 695px, About 598px — the floor…"
    ],
    "required": false
}
