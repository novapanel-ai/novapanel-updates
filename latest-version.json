{
    "version": "1.1.169",
    "downloadUrl": "https://github.com/novapanel-ai/novapanel-updates/releases/download/v1.1.169/NovaPanel-v1.1.169.zip",
    "notes": [
        "Fixed a real bug found during CEO hands-on testing of the new sensor service: SensorBridge prints its backend-availability banner exactly once, right after it starts. Under the old direct-spawn model that was fine (fresh spawn every…",
        "Installer upgrade safety: the service is now stopped before files are copied during an upgrade, not just started after. Previously an upgrade could silently copy a new NovaSensorService.exe while the old version kept running from the…",
        "Removed a dead-end duplicate: sensor gaps (like the one above) were being shown twice — once correctly on the actual CPU/GPU card, and again as a 'Setup Action' in the Recommended Tools list with no button, since it never had a URL.…",
        "Fixed 'Needs Setup' being shown for gaps nothing can actually fix — e.g. a GPU that genuinely doesn't expose temperature/clock/power to any sensor interface NovaPanel can read. That's a hardware/driver ceiling, not a setup problem. Now…"
    ],
    "required": false
}
