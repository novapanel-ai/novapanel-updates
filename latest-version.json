{
    "version": "1.1.445",
    "downloadUrl": "https://github.com/novapanel-ai/novapanel-updates/releases/download/v1.1.445/NovaPanel-v1.1.445.zip",
    "notes": [
        "Text tab's font dropdown no longer offers Georgia or Courier New — both run noticeably wider per character than the panel's other font choices and can overflow the card.",
        "Fixed a real underlying gap: a reading's value could grow wider than its row instead of yielding to it (a flex-layout default, not font-specific), which is what let text run past the card edge. Now it truncates with an ellipsis instead."
    ],
    "required": false
}
