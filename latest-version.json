{
    "version": "1.1.572",
    "downloadUrl": "https://github.com/novapanel-ai/novapanel-updates/releases/download/v1.1.572/NovaPanel-v1.1.572.zip",
    "notes": [
        "Extended the diff past tab content to the window chrome, which had never been checked. The footer sits on the wrong surface — it declares no background of its own, so it fell back to chrome/darker instead of the design's raised/lighter…",
        "Hover states, checked for the first time this session by triggering a REAL :hover with a raw CDP mouseMoved event rather than a synthetic DOM event (which cannot trigger :hover at all — confirmed the hard way first). Two gaps found: a…",
        "The tab-bar hover fix is scoped to #tabbar specifically, since .tab-btn is shared with rail contexts this was never meant to touch."
    ],
    "required": false
}
