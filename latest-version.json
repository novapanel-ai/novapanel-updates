{
    "version": "1.1.645",
    "downloadUrl": "https://github.com/novapanel-ai/novapanel-updates/releases/download/v1.1.645/NovaPanel-v1.1.645.zip",
    "notes": [
        "Fleet Command (NP-CTRL-01's display) now shows restart count and smoke-test result per unattended machine, when that machine actually produces the data. Nothing is shown for a node that doesn't run the watchdog or the smoke harness -- no…",
        "The unattended-machine watchdog now logs every time it actually had to restart the app, not just whether it's up right now. That's what feeds the restart count above; it only counts a genuine restart, never a tick where the app was already…"
    ],
    "required": false
}
